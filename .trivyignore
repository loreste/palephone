# Transitive via aws-sdk-s3 / aws-config (aws-smithy-http-client still pulls
# rustls 0.21 legacy path). ldap3 is on 0.12 / rustls 0.23.
# Tracked: GHSA-82j2-j2ch-gfr8 (DoS on malformed CRL BIT STRING).
# Re-evaluate when AWS SDK drops rustls 0.21 entirely.
GHSA-82j2-j2ch-gfr8
